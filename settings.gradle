rootProject.name = "omp-sample-service"

// TO BE REMOVED
def howToInitProject() {
    println "1. Please read README.md to follow package introduction before starting development"
    println "2. Change rootProject.name in settings.gradle, please keep it same with gitlab project name"
    println "3. Change group, version and description in build.gradle if needed"
    println "4. (Optional) Remove section `Caching` in build.gradle if no redis is required"
    println "5. (Optional) Remove section `swagger` in build.gradle if no swagger is required"
    println "6. (Optional) Remove dependency `mybatis-support-library` in build.gradle if no mybatis is required"
    println "7. (Optional) Remove dependency `spring-web-support-library` in build.gradle if no spring mvc is required"
    println "8. (Optional) Remove dependency `spring-security-support-library` in build.gradle if no spring security is required"
    println "9. Change tags and project in logback-spring.xml, please keep them same with rootProject.name"
    println "10. Change `cn.porsche.omp` to actual package name in logback-spring.xml"
    println "11. Change spring.application.name in bootstrap.yml, please keep them same with rootProject.name"
    println "12. Change database name to actual database name in application-local.yml"
    println "13. (Optional) Remove section `redis` in application-local.yml if no redis is required"
    println "14. Change sub package `omp` of `cn.porsche` to actual package name in package `src` and `test`"
    println "15. Change `cn.porsche.omp` to actual package name in scanBasePackages of Application.java"
    println "16. Change `title` and `basePackage` in SwaggerConfig.java"
    println "17. Change `omp-sample-service` to actual rootProject.name in 2 file `Dockerfile` in DockerDir"
    println "18. Change `omp-sample-service` to actual rootProject.name in 4 file in kubedir"
    println "19. Change `porsche_omp_sample` to actual database name to in .gitlab-ci.yml"
    println "20. Remove line 3 - 27 from settings.gradle after step 1-19 are done"
}
howToInitProject() 
